
import {NgModule} from '@angular/core';


import {HloStats} from './hlo_stats';

/** An HLO stats module. */
@NgModule({
  imports: [HloStats],
  exports: [HloStats],
})
export class HloStatsModule {
}
