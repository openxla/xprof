import {NgModule} from '@angular/core';

import {InferenceProfile} from './inference_profile';

@NgModule({
  imports: [InferenceProfile],
  exports: [InferenceProfile],
})
export class InferenceProfileModule {
}
